# Library Management (Flask + SQLAlchemy)


A minimal library management system for learning Flask and SQLAlchemy.


## How to run locally


1. Create a virtual environment:


```bash
python -m venv venv
source venv/bin/activate # macOS/Linux
venv\Scripts\activate # Windows (PowerShell)